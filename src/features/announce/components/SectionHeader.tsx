function SectionHeadr({ children }: { children: React.ReactNode }) {
  return (
    <div className="tracking-tight-custom text-gray2 text-[20px] font-medium">
      {children}
    </div>
  );
}

export default SectionHeadr;
